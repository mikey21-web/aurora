{
  "name": "aurora-extension",
  "version": "2.0.0",
  "description": "Aurora browser extension for cookie-based platform authentication",
  "scripts": {
    "build": "vite build && node scripts/package-extension.mjs",
    "dev": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && cross-env HOT_RELOAD_EXTENSION_VITE_PORT=8081 NODE_ENV=development dotenv -e ../../.env -- vite build --config vite.config.chrome.ts --mode development --watch"
  },
  "type": "module"
}
